%h1 Portfolio items
=link_to 'Created new item', new_portfolio_url
-@portfolio_items.each  do |portfolio|
  %p=link_to portfolio.title, portfolio_path(portfolio)
  %p=portfolio.subtitle
  %p=portfolio.body
  =image_tag portfolio.thumb_image unless portfolio.thumb_image.nil?
  =link_to 'Edit', edit_portfolio_path(portfolio)

